services:
  # Service สำหรับ PostgreSQL database ใช้เก็บข้อมูลเชิง relational
  postgres:
    image: postgres:16
    container_name: postgres
    restart: unless-stopped
    # env_file: ./.env
    environment:
      - POSTGRES_USER=admin
      - POSTGRES_PASSWORD=1234
      - POSTGRES_DB=appdb
    ports:
      - "5432:5432"
    volumes:
      - pg_data:/var/lib/postgresql/data

  # Service สำหรับ etcd ซึ่งเป็น distributed key-value store สำหรับเก็บข้อมูลเกี่ยวกับการ configuration
  etcd:
    container_name: milvus-etcd
    image: quay.io/coreos/etcd:v3.5.5
    environment:
      - ETCD_AUTO_COMPACTION_MODE=revision
      - ETCD_AUTO_COMPACTION_RETENTION=1000
      - ETCD_QUOTA_BACKEND_BYTES=4294967296
      - ETCD_SNAPSHOT_COUNT=50000
    volumes:
      - ${DOCKER_VOLUME_DIRECTORY:-.}/volumes/etcd:/etcd
    command: etcd -advertise-client-urls=http://127.0.0.1:2379 -listen-client-urls http://0.0.0.0:2379 --data-dir /etcd
    healthcheck:
      test: ["CMD", "etcdctl", "endpoint", "health"]
      interval: 30s
      timeout: 20s
      retries: 3

  # Service สำหรับ Minio ใช้สำหรับ object storage ที่เข้ากันได้กับ Amazon S3 API
  minio:
    container_name: milvus-minio
    image: minio/minio:RELEASE.2023-03-20T20-16-18Z
    environment:
      MINIO_ACCESS_KEY: minioadmin
      MINIO_SECRET_KEY: minioadmin
    ports:
      - "9001:9001"  # พอร์ตสำหรับ Minio console
      - "9000:9000"  # พอร์ตหลักสำหรับ Minio
    volumes:
      - ${DOCKER_VOLUME_DIRECTORY:-.}/volumes/minio:/minio_data
    command: minio server /minio_data --console-address ":9001"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/live"]
      interval: 30s
      timeout: 20s
      retries: 3

  # Service สำหรับ Milvus ที่ทำงานในโหมด standalone สำหรับการจัดการ vector similarity search
  standalone:
    container_name: milvus-standalone
    image: milvusdb/milvus:v2.4.6
    command: ["milvus", "run", "standalone"]
    security_opt:
      - seccomp:unconfined
    environment:
      ETCD_ENDPOINTS: etcd:2379
      MINIO_ADDRESS: minio:9000
    volumes:
      - ${DOCKER_VOLUME_DIRECTORY:-.}/volumes/milvus:/var/lib/milvus
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9091/healthz"]
      interval: 30s
      start_period: 90s
      timeout: 20s
      retries: 3
    ports:
      - "19530:19530"  # พอร์ตหลักของ Milvus สำหรับ API
      - "9091:9091"    # พอร์ตสำหรับตรวจสอบสถานะ health
    depends_on:
      - "etcd"
      - "minio"

  # Service สำหรับดูข้อมูลใน Milvus โดยใช้ Attu GUI
  attu:
    image: zilliz/attu:v2.2.6
    container_name: attu
    ports:
      - "3000:3000"
    environment:
      MILVUS_URL: "milvus-standalone:19530"

volumes:
  pg_data:
  etcd:
  minio:
  milvus:

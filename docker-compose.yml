services:
  backend:
    build: ./backend
    container_name: backend
    env_file: ./backend/.env
    ports:
      - "8001:8001"
    restart: unless-stopped
    command: >
      uvicorn client:app
      --host 0.0.0.0
      --port 8001
      --workers 1
      --timeout-keep-alive 30
